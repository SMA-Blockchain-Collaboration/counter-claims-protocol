// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "./ClaimEarth";

contract DeployEarth {
  event CreatedEarth(address account, )

  //deploys the single Earth Token, and emits an event on deployment
  function createEarth(){
    emit CreatedEarth()
  }

  //returns the address of the token on the chain
  function getAddress(){

  }
}
